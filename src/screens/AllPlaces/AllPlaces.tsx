import { View, Text } from "react-native";
import React from "react";
import PlacesList from "../../components/Places/PlacesList/PlacesList";

const AllPlaces = () => {
  return <PlacesList places={null} />;
};

export default AllPlaces;
